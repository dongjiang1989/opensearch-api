apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "opensearch-file-api.serviceAccountName" . }}
  labels:
    {{- include "opensearch-file-api.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
